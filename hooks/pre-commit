#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

GREEN=$(tput setaf 2)
NORMAL=$(tput sgr0)

echo "${GREEN}---------------------PRE-COMMIT---------------------${NORMAL}"

yarn lint-staged --verbose --config .lintstagedrc.json
